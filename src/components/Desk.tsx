import { View, Text } from "react-native";

const Desk = () => {
  return (
    <View
      style={{
        width: 25,
        height: 25,
        backgroundColor: "gray",
      }}
    ></View>
  );
};

export default Desk;
